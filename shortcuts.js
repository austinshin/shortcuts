/*
 * @author: Austin Shin
 * A shortcuts presentation
 * This was done in sublime text 3
 * Many of these are applicable to vscode/atom.
 */

class shortcuts {
  constructor() {}

  // alt + shift + up/down to move around
  movingUpAndDownOneRow() {
    let x = 0;
    let x = 1;
    let x = 2;
  }

  deleteRow() {
  	return 'cmd + backspace';
    return 'cmd + shift + left/right;
    return 'cmd + shift + left/right || cmd + backspace';
  }

  fixMe() {
  	const x = 5;
  	x = 4;
  	return 'alt + shift + left/right';
  }

  fixIndents() {
let hrsf87chant = "hr87!";
hrsf87chant = "Crank it to Eleven";
hrsf87chant = "There's nobody named Kevin";
      hrsf87chant = "Shoutout to McLovin";
		hrsf87chant = ":O";
		return 'fn + tab' || 'fn + shift tab || cmd + [ or ]';
  }

  commentThisOut() {
    Hello
    How
    Are
    You
    return 'cmd + / || cmd + alt + /'
  }

// ctrl + g || cmd + t || cmd + p












  goToLineNumber() {
  	//make this true
    let x = 'FILL ME IN';
    return x === 'Hello!';
  }

  goToTopBot() {
    return 'cmd + down' || 'cmd + up';
  }

  goToBeginningOrEnd() {
    return 'cmd + left' || 'cmd + right';
  }


  removeTodosMultipleTextHighlight() {
  	//TODO
  	//TODO
  	//TODO
  	return 'cmd + d || cmd + u';
  }

  consoleLogMultipleThings() {



    return 'cmd + shift + L'
  }


  readjustLines() {
  	y = x + 3;
    let y = 1;
    let x = 1 + 1;
    return y || 'cmd + ctrl + up/down';
  }

  closeThisFileThenReopn() {
  	return 'cmd + s -> cmd + w -> cmd + shift + t';
  }

  splitPane() {
    return 'cmd + alt + 2';
  }

/*
google chrome hotkeys - many of these applies to terminal too
highlight search bar - cmd + L
bookmarking localhost:3000 or whatever for quick access
move tab right - ctrl + tab
move tab left - ctrl + shift + tab
cmd + 1 = go to first tab
cmd + 9 = go to last tab
cmd + 2-8 = numbers (tab organization)

cmd line
pause whatever you are doing -> ctrl z
bring back -> fg
cmd d for split console vertically (iterm)
cmd shift d for split console horizontally (iterm)
use screen or tmux in default terminal

make a file and use nodemon on it for consistent testing.

if you want to test something quickly and want to install npm packages
mkdir testdir
npm init -y
npm install 'whatever you need'
touch test.js //do whatever you want to test
node(mon) test.js
you can also install mocha/chai to test too
*/

}

